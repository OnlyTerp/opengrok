---
name: New provider map
about: A provider's wire behavior is wrong or missing in opengrok
title: "map: <provider> — "
labels: provider-map
---

**Provider + base URL:** 

**Model slug:** 

**What feels off:** <!-- slow? dumb? token-hungry? effort ignored? -->

**Do you have a wire capture?**
- [ ] yes — attach `capture.json` + `verdict.json` from `python tools/wire-probe.py`
- [ ] no — describe expected vs actual behavior and we'll probe

**Expected behavior** (what the provider's own harness sends):

**Actual behavior** (what Grok Bot produces today):
